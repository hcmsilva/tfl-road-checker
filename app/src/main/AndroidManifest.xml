<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.demos.henrique.tflroadchecker">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/tfl_roundel"
            android:label="@string/app_name"
            android:roundIcon="@drawable/tfl_roundel"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".simpleroadchecker.SimpleRoadCheckerActivity"
                android:label="@string/title_activity_simple_road_checker"
                android:theme="@style/AppTheme.NoActionBar"
                android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>